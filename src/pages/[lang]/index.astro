---
import Layout from '../../layouts/Layout.astro';

export function getStaticPaths() {

  return [
    // { params: { lang: 'en' } },
    // { params: { lang: 'es' } },
  ];
}

// const { lang } = Astro.params;

---

<Layout title="Martín Fogliacco | Portfolio">
	<main>
    Hello World
	</main>
</Layout>

